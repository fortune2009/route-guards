import 'package:flutter/material.dart';

class AddProductsRoute extends StatelessWidget {
  const AddProductsRoute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Dashboard'),
    );
  }
}
